# ERP Task Manager (без базы данных)

Локальное приложение для учёта производственных заказов.  
Все данные хранятся **только в вашем браузере** (`localStorage`).

## Возможности
- Создание заказов с гибкими этапами
- Подтверждение этапов
- Комментарии и фотоотчёты (встроенные в JSON)
- Экспорт/импорт данных в файл `.json`

## Ограничения
- Работает только в одном браузере
- Нет совместной работы
- Данные можно потерять при очистке истории

## Как использовать
1. Откройте `index.html` в браузере
2. Создавайте заказы
3. Регулярно делайте **Экспорт данных** (на случай сбоя)

> 💡 Чтобы открыть правильно:  
> ```bash
> npx serve
> ```
> или просто двойной клик по `index.html`.

Автоматически сохраняется в `localStorage`.
